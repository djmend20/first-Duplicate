# first-Duplicate
The program is one of the solutions to a coding problem provided by code singal. 

The problem presented by CodeSingal is given an array that ranged from 1 to the array length code a 
solution that returns the first duplicate. If no duplicates are presented then the solution returns -1. 
